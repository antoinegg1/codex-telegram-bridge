{
  "name": "codex-telegram-bridge",
  "version": "0.1.0",
  "description": "Telegram bridge for local Codex stop, decision, and continuation notifications.",
  "type": "module",
  "bin": {
    "codex-telegram-bridge": "./dist/main.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node ./node_modules/typescript/bin/tsc -p tsconfig.json",
    "typecheck": "node ./node_modules/typescript/bin/tsc -p tsconfig.json --noEmit",
    "test": "node ./node_modules/vitest/vitest.mjs run",
    "prepare": "npm run build"
  },
  "keywords": [
    "codex",
    "telegram",
    "cli",
    "bot"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
